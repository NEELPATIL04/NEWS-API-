first clone setup branch from git repo
check public and src complete files clone properly 
then you have to change api key newsitemcontrol.jsx 
sign newsapi.org get api key
then React normal setup 
then run command "npm run dev"
load spinner already in the file external setup is there follow the step and run
